[build]
phpExtensions = ["ctype", "curl", "dom", "fileinfo", "filter", "hash", "mbstring", "openssl", "pcre", "pdo", "pdo_mysql", "session", "tokenizer", "xml", "redis"]

[deploy]
preDeployCommand = "php artisan config:clear && cp .env.example .env && php artisan migrate --force"
